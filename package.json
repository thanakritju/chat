{
  "name": "chat",
  "version": "1.0.0",
  "description": "Counter page with persistent labels and values",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
